{% extends 'base.html.twig' %}

{% block title %}Platform{% endblock %}

{% block body %}
    <h1>Platform</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ platform.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ platform.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_backoffice_platform_index') }}">Retour à la liste</a>

    <a href="{{ path('app_backoffice_platform_edit', {'id': platform.id}) }}">Modifier</a>

    {{ include('platform/_delete_form.html.twig') }}
{% endblock %}
