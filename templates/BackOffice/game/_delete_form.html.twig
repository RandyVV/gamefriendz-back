<form method="post" action="{{ path('app_backoffice_game_delete', {'id': game.id}) }}" onsubmit="return confirm('Es-tu sûr de vouloir supprimer ce jeu de la base de données ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ game.id) }}">
    <button class="btn btn-danger" type="submit">Supprimer</button>
</form>
